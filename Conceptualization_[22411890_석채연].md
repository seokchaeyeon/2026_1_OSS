# Conceptualization Report: White-box FDS

## 1. Business purpose

**1.1 Project Background**
- 자금 세탁이나 온라인 금융 사기가 증가한 최근 금융권 및 핀테크 산업에서는 딥러닝과 같은 AI 기반의 이상 거래 탐지 시스템(FDS) 도입이 활발하다. 그러나 고도화된 AI 모델은 왜 특정 거래를 이상 거래로 분류했는지 그 논리적 근거를 도출하기 어려운 블랙박스 문제를 내포하고 있다. 금융 규제가 강화되고 소비자 보호가 중요해지는 현재 고객의 자산을 동결하거나 거래를 제한할 때 명확한 사유를 빠르게 설명할 수 없는 점은 CS 불만 증가와 컴플라이언스 리스크로 이어지고 있다.

**1.2 Motivation**
- 이러한 블랙박스 모델의 한계를 극복하기 위해 과정과 결과가 100% 추적 가능하며 투명한 화이트박스 FDS의 필요성을 인식하였다. 금융 사기라고 판단되는 특정 조건에 부합한 데이터 조합을 정확히 찾아내고 관련 부서로 공유할 수 있도록 프로젝트를 기획하였다.

**1.3 Goal**
- 프로젝트의 최종 목표는 자바(Java) 기반의 설명 가능한 이상 거래 탐지 룰 엔진 백엔드를 개발하는 것이다.

   1. 실시간으로 유입되는 방대한 거래 데이터 객체들을 효율적으로 처리하고 정렬하는 파이프라인 구축.
   2. 스머핑(Smurfing) 같은 금융 범죄를 탐지하기 위해 전체 거래 내역 중 특정 금액의 합을 만들어내는 요소들의 조합을 역추적하는 탐지 알고리즘 구현.
   3. 이상 거래 적발 시 위반한 특정 규칙과 증거 데이터를 명확한 로그(Log) 형태로 반환하는 시스템 구축.

**1.4 Target Market**
- 명확한 거래 제한 사유를 고객과 금융 당국에 설명해야 하는 시중 은행 및 대형 핀테크 기업의 컴플라이언스 부서 및 CS 관리팀.


---

## 2. System context diagram

<img width="640" height="160" alt="image" src="https://github.com/user-attachments/assets/be47522b-3853-4bc1-9780-5ba53c643e38" />

                              [그림1]White-box FDS Diagram

- **White-box Rule-based FDS Engine (System):** 본 프로젝트에서 개발하는 핵심 시스템 경계. 외부로부터 실시간 거래 데이터를 배열 등의 자료구조 형태로 입력받아 신속하게 정렬. 이후 특정 금액의 합을 만들어내는 거래 조합을 탐색하는 등 사전 정의된 규칙에 따라 이상 거래 여부를 판별하고 그 위반 근거를 명확한 데이터 로그로 생성.
- **Core Banking System (External System):** 은행이나 핀테크 기업이 운영하는 기존의 코어 금융 시스템. 고객이 발생시킨 결제 및 이체 데이터를 FDS 엔진으로 push, FDS 엔진의 판별 결과에 따라 실제 계좌의 출금을 승인하거나 차단하는 역할 수행.
- **CS Administrator (User):** 시스템의 최종 사용자인 은행의 고객 서비스 담당자 및 컴플라이언스 관리자. FDS 엔진이 도출한 투명한 위반 로그를 대시보드를 통해 확인하고 이를 바탕으로 금융 소비자에게 정확한 거래 제한 사유 안내.

---

## 3. Use case list

### 3.1. 관리자 시스템 접속 (로그인)
| Actor | CS Administrator |
| :--- | :--- |
| **Description** | 관리자가 아이디와 비밀번호를 입력하여 FDS 대시보드 시스템에 접속(로그인)한다. |

### 3.2. 실시간 거래 데이터 수신
| Actor | Core Banking System |
| :--- | :--- |
| **Description** | 코어 뱅킹 시스템이 사용자의 결제 및 이체 데이터를 FDS 엔진으로 실시간 전송한다. |

### 3.3. 데이터 파싱 및 정렬 처리
| Actor | FDS Engine |
| :--- | :--- |
| **Description** | 시스템이 수신된 데이터를 내부 객체로 변환하고 탐색 속도를 높이기 위해 즉시 시간순 및 금액순으로 배열에 정렬한다. |

### 3.4. 스머핑(분할 송금) 위험 탐지
| Actor | FDS Engine |
| :--- | :--- |
| **Description** | 시스템이 정렬된 배열을 순회하며 서로 다른 두 거래 금액 요소의 합이 특정 제한 한도와 일치하는지 계산하여 우회 송금을 탐지한다. |

### 3.5. 단기간 고액 거래 위험 탐지
| Actor | FDS Engine |
| :--- | :--- |
| **Description** | 시스템이 짧은 시간 내에 평소 패턴을 벗어난 고액 결제가 연속으로 발생하는지 룰에 따라 검사한다. |

### 3.6. 이상 거래 자동 차단 (보류)
| Actor | FDS Engine |
| :--- | :--- |
| **Description** | 3.4나 3.5에서 이상 거래로 판별될 경우 시스템이 해당 거래의 승인을 거절하고 상태를 보류(Blocked)로 전환한다. |

### 3.7. 화이트박스 위반 증거 로그 생성
| Actor | FDS Engine) |
| :--- | :--- |
| **Description** | 시스템이 차단된 거래에 대해 위반한 룰의 명칭과 증거가 되는 거래 내역 조합을 투명한 데이터 로그 형태로 저장한다. |

### 3.8. 관리자에게 위험 알림
| Actor | FDS Engine |
| :--- | :--- |
| **Description** | 시스템이 이상 거래를 차단함과 동시에 관리자 대시보드에 즉각적인 위험 알림을 보낸다. |

### 3.9. 실시간 대시보드 모니터링
| Actor | CS Administrator |
| :--- | :--- |
| **Description** | 관리자가 대시보드를 통해 현재 시스템에 유입되는 전체 거래의 정상/이상 판별 현황을 실시간으로 모니터링한다. |

### 3.10. 위반 증거 로그 상세 조회
| Actor | CS Administrator |
| :--- | :--- |
| **Description** | 관리자가 알림을 클릭하여 시스템이 생성한 특정 고객의 위반 증거 로그(조합된 결제 내역 등)를 상세하게 확인한다. |

### 3.11. 고객 계좌 수동 제어 (잠금/해제)
| Actor | CS Administrator |
| :--- | :--- |
| **Description** | 관리자가 상세 로그 확인 후 정상 거래로 판단되면 차단을 해제하고 사기로 판단되면 계좌를 잠금 처리한다. |

### 3.12. 탐지 룰 추가 및 수정
| Actor | CS Administrator |
| :--- | :--- |
| **Description** | 관리자가 새로운 사기 수법에 대응하기 위해 시스템에 새로운 검증 로직을 추가하거나 금액 한도를 수정한다. |

---



## 4. Concept of operation

**4.1. 관리자 시스템 접속 (로그인)**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | FDS 대시보드 사용을 위해 허용된 관리자인지 확인 |
| **Approach** | 관리자가 아이디와 비밀번호를 입력하면 시스템이 권한을 검증하여 접속을 승인한다. |
| **Dynamics** | 관리자가 시스템 웹페이지에 처음 접속하여 로그인을 시도할 때 |
| **Goals** | 안전한 관리자 인증 및 세션 관리 기능을 구현한다. |

**4.2. 실시간 거래 데이터 수신**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 외부에서 발생하는 금융 거래 내역을 실시간으로 수집 |
| **Approach** | 코어 뱅킹 시스템이 사용자의 결제/이체 데이터를 FDS 엔진으로 즉시 전송한다. |
| **Dynamics** | 사용자가 실제 금융 거래(결제, 송금 등)를 발생시킬 때마다 |
| **Goals** | 외부 시스템과의 안정적인 실시간 데이터 수신 파이프라인을 구축하고 이상 거래를 빠르게 인지하기 위해 거래가 발생할 떄마다 즉시 전송한다. |

**4.3. 데이터 파싱 및 정렬 처리**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 수신된 데이터를 분석하기 쉬운 형태로 가공하고 탐색 속도를 최적화 |
| **Approach** | 원본 데이터를 자바(Java) 객체로 파싱한 후 배열 내에서 시간순 및 금액순으로 정렬한다. |
| **Dynamics** | 새로운 거래 데이터가 FDS 엔진 파이프라인에 수신된 직후 |
| **Goals** | 빠르고 효율적인 데이터 탐색을 위한 자료구조 및 알고리즘을 구현한다. |

**4.4. 스머핑(분할 송금) 위험 탐지**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 고액 거래 제한을 피하기 위해 금액을 쪼개어 송금하는 우회 범죄 패턴 적발 |
| **Approach** | 정렬된 배열을 순회하며 서로 다른 두 거래 요소의 합이 특정 제한 한도와 일치하는지 또는 같은 목적지로 여러번의 송금이 발생했는지 계산한다. |
| **Dynamics** | 정렬된 거래 데이터가 시스템 내부의 룰 엔진을 통과할 때 |
| **Goals** | 배열 기반의 탐색 알고리즘을 활용한 스머핑 범죄 탐지 로직을 구현한다. |

**4.5. 단기간 고액 거래 위험 탐지**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 평소 패턴을 벗어난 비정상적인 단기간 고액 결제 시도 차단 |
| **Approach** | 특정 시간(예: 10분) 내의 누적 거래 금액을 합산하여 룰 엔진에 설정된 임계값(소비자의 평균 사용 금액) 초과 여부를 검사한다. |
| **Dynamics** | 정렬된 거래 데이터가 시스템 내부의 룰 엔진을 통과할 때 |
| **Goals** | 시간과 금액 조건을 결합한 복합 이상 탐지 기능을 구현한다. |

**4.6. 이상 거래 자동 차단 (보류)**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 탐지된 위험 거래가 실제 승인되어 금전적 피해가 발생하는 것을 방지 |
| **Approach** | 룰 엔진에서 위험이 감지되면 즉시 해당 거래의 승인을 거절하고 상태를 '보류(Blocked)'로 변경한다. |
| **Dynamics** | 스머핑 또는 고액 거래 탐지 룰에서 이상이 적발되었을 때 즉시 |
| **Goals** | 금융 거래의 정합성을 지키는 실시간 제어 로직을 구현한다. |

**4.7. 화이트박스 위반 증거 로그 생성**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 거래 차단 사유에 대한 투명하고 설명 가능한 근거 데이터 확보 |
| **Approach** | 차단된 거래와 그 원인이 된 룰의 명칭 조합된 거래 내역 등 증거 데이터를 로그 포맷으로 저장한다. |
| **Dynamics** | 이상 거래가 시스템에 의해 자동 차단(보류) 처리된 직후 |
| **Goals** | 추적 가능하고 투명한 화이트박스 데이터 로깅 시스템을 구현한다. |

**4.8. 관리자에게 위험 알림**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 이상 거래 발생 사실을 CS 관리자에게 즉시 인지시킴 |
| **Approach** | 위험 거래가 차단되면 대시보드 화면에 실시간 경고 알림이나 시각 또는 청각 강조 효과를 발생시킨다. |
| **Dynamics** | 시스템에 새로운 위반 증거 로그가 생성되었을 때 |
| **Goals** | 관리자의 빠른 대응을 돕는 실시간 알림 기능을 구현한다. |

**4.9. 실시간 대시보드 모니터링**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | FDS 시스템의 전체적인 가동 상태 및 정상/이상 거래 현황 파악 |
| **Approach** | 전체 거래 현황 및 룰 위반 통계 등을 시각화하여 대시보드 화면에 지속적으로 업데이트한다. |
| **Dynamics** | 관리자가 시스템 메인 대시보드에 접속하여 현황을 모니터링할 때 |
| **Goals** | 데이터 현황을 직관적으로 파악할 수 있는 관리자 이미지를 제공한다. |

**4.10. 위반 증거 로그 상세 조회**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 관리자가 특정 이상 거래의 차단 사유를 정확히 분석하고 고객에게 안내 |
| **Approach** | 알림을 클릭하면 저장된 화이트박스 로그(어떤 규칙을 위반했고, 어떤 금액이 조합되었는지)를 상세히 출력한다. |
| **Dynamics** | 관리자가 특정 이상 거래 건의 상세 데이터 분석을 요청할 때 |
| **Goals** | 로깅된 데이터를 가독성 있게 보여주는 데이터 조회 기능을 구현한다. |

**4.11. 고객 계좌 수동 제어 (잠금/해제)**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 관리자의 최종 판단 결과에 따른 고객 계좌 상태의 물리적 변경 처리 |
| **Approach** | 관리자가 증거를 바탕으로 대시보드에서 잠금 또는 해제를 선택하면 코어 시스템에 명령을 전송한다. |
| **Dynamics** | 관리자가 증거 로그 분석을 마치고 실제 사기 여부에 대해 최종 확정했을 때 |
| **Goals** | 시스템의 오류를 보완하고 계좌의 안전을 최종 보장하는 수동 제어 기능을 구현한다. |

**4.12. 탐지 룰 추가 및 수정**
| 항목 | 내용 |
| :--- | :--- |
| **Purpose** | 새롭게 진화하는 금융 범죄 수법에 유연하고 즉각적으로 대응 |
| **Approach** | 관리자가 새로운 타겟 금액이나 시간 조건을 입력하면 시스템 중단 없이 내부 룰 엔진의 변수를 업데이트한다. |
| **Dynamics** | 새로운 사기 패턴이 발견되어 관리자가 탐지 기준을 변경하고자 할 때 |
| **Goals** | 유지보수성이 뛰어나고 유연하게 확장 가능한 동적 룰 엔진을 구현한다. |

---

## 5. Problem statement

### 5.1. Overview
White-box FDS는 코어 뱅킹 시스템으로부터 유입되는 방대한 결제 및 이체 데이터를 실시간으로 모니터링하고 이상 거래를 차단하는 것이 주 목적이다. 또한 기존 블랙박스 AI 모델과 달리 차단된 거래에 대해 명확한 위반 규칙과 증거 데이터를 로그로 남겨 CS 관리자가 투명하게 고객 응대에 활용할 수 있게 하는 것이 장점이다.

이 시스템은 다음과 같은 목적이 달성할 수 있어야 한다.
- 대용량 거래 데이터의 실시간 정렬 및 처리
- 스머핑 등 복잡한 우회 거래 패턴의 정확한 탐지
- 100% 추적 가능한 화이트박스 기반의 증거 데이터 제공

### 5.2. Problem definition
White-box FDS에서 대규모 거래 데이터를 실시간으로 처리하고 룰을 검증하는 과정에서 직면할 수 있는 문제점 및 해결 방법은 다음과 같다.

#### 5.2.1. Problem#1 Time complexity of Smurfing detection algorithm (스머핑 탐지 알고리즘의 시간 복잡도)
고액 거래 제한을 피하기 위해 금액을 분할하여 송금하는 스머핑 범죄를 탐지하기 위해서는 사용자의 거래 내역 배열 내에서 서로 다른 두 결제 요소의 합이 특정 제한 한도와 일치하는지 모든 가능한 조합을 찾아내야 한다.
하지만 데이터가 대량으로 누적될 경우 단순탐색 알고리즘을 이용하면 병목 현상이 발생하므로  빠르고 정확한 실시간 탐지를 위해 알고리즘을 다음과 같은 방식으로 최적화한다.
- 먼저 시스템에 유입된 거래 데이터 객체들을 금액순 또는 시간순으로 시간복잡도가 낮은 알고리즘을 이용하여 빠르게 정렬한다.
- 정렬된 배열을 해시맵 자료구조를 활용하거나 속도가 빠른 탐색 알고리즘을 활용해 특정 합을 만들어내는 거래 조합을 효율적으로 탐색해 낸다.

#### 5.2.2. Problem#2 Explainable logging data structure (투명한 증거 로그 데이터 구조화)
기존 시스템은 위험도(%)만을 반환하기 때문에 정확한 통신과 증명을 위해서는 서버와 관리자 간에 주고받는 위반 증거 데이터의 규격(프로토콜)을 명확히 정하는 것이 필수적이다.
또한 소비자의 거래 내역 데이터를 사용하는 것이기 때문에 인권 침해의 문제도 발생할 수 있다.
White-box FDS에서는 투명한 근거 제공을 위해 로그 데이터를 다음과 같은 방식으로 구조화한다.
- 차단 발생 시 단순한 보류 상태값만 넘기는 것이 아니라 가장 먼저 위반한 규칙(예: 분할 송금 제한)과 제한 한도를 명시한다.
- 그 뒤에는 해당 룰에 적발된 실제 결제 내역 객체(배열 내에서 합이 일치했던 요소들)들을 증거 배열에 담아 하나의 온전한 위반 로그 객체로 반환한다.

#### 5.2.3. Problem#3 Real-time data sorting pipeline (실시간 데이터 정렬 파이프라인)
코어 뱅킹 시스템에서 거래 데이터는 한 번에 들어오는 것이 아니라 실시간으로 끊임없이 유입된다. 매번 새로운 데이터가 들어올 때마다 전체 배열을 처음부터 다시 정렬하는 것은 시스템 리소스를 크게 낭비한다.
따라서 실시간 데이터 처리를 위해 다음과 같은 방식을 적용한다.
- 이미 정렬되어 있는 기존 거래 내역 배열에 새로운 결제 건이 들어올 경우 삽입 정렬의 원리를 응용하여 새로운 데이터가 즉시 자신의 알맞은 위치를 찾아 들어가도록 파이프라인을 설계한다.

---


## 6. Glossary

* **FDS (Fraud Detection System, 이상 거래 탐지 시스템):** 코어 뱅킹 시스템에서 발생하는 결제 및 이체 데이터를 실시간으로 분석하여 사기, 해킹, 자금 세탁 등의 비정상적인 패턴을 탐지하고 차단하는 보안 시스템.
* **Smurfing (스머핑):** 자금 추적이나 고액 거래 제한을 피하기 위해 큰 금액을 여러 개의 작은 소액으로 쪼개어 시간차를 두고 송금하거나 여러 계좌로 분산시키는 금융 범죄 수법.
* **White-box Model (화이트박스 모델):** 시스템이 특정 결과를 도출해 낸 내부 로직과 의사결정 과정을 사용자가 명확하게 역추적하고 이해할 수 있는 투명한 시스템 구조. (결과만 도출하고 과정을 알 수 없는 Black-box 모델과 대비됨)
* **Rule Engine (룰 엔진):** 사전에 정의된 비즈니스 규칙(조건문, 알고리즘, 임계값 등)을 입력된 데이터에 적용하여 해당 데이터가 규칙을 위반했는지 판별하고 특정 결과를 도출하는 독립적인 소프트웨어 컴포넌트.
* **CS:** 고객서비스
* **Hash Map(해시맵):**  키(Key)와 값(Value)을 쌍으로 매핑하여 데이터를 저장하는 자료구조. 데이터 검색 및 접근 속도(O(1))가 매우 빨라 방대한 결제 데이터 배열 속에서 특정 금액을 만족하는 거래 요소의 조합을 실시간으로 빠르게 탐색하고 매칭할 때 활용되는 핵심 기술.
* **Pipeline (파이프라인):** 외부 코어 뱅킹에서 유입된 원시 데이터가 최종 목적을 달성할 때까지 여러 독립적인 처리 단계(데이터 수신 -> 객체 파싱 -> 배열 정렬 -> 룰 검사 -> 로깅 및 차단)를 컨베이어 벨트처럼 순차적으로 거치며 자동 처리되는 데이터 흐름 아키텍처.
* **Insertion Sorting (삽입 정렬):** 데이터가 새로 추가될 때 전체 배열을 처음부터 다시 정렬하는 대신 이미 정렬되어 있는 배열 구조 내에서 새로운 데이터의 알맞은 위치를 찾아 곧바로 삽입하는 정렬 알고리즘. 실시간으로 끊임없이 유입되는 단건의 결제 데이터를 기존 데이터 배열에 빠르게 편입시키는 데 매우 효율적이다.

---

## 7. References

* [1] 금융보안원, 이상거래 탐지시스템(FDS) 운영 가이드라인
* [2] 정의석,전자금융 이상징후 거래 지능화(AI) 탐지 모델연구
